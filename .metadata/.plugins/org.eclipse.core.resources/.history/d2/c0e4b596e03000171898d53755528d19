import java.io.File;
import java.nio.file.*;
import java.util.*;

public class Indexing {
	
	public void execute(){
		/*
		 * TODO:
		 * 	Main de Indexacion:
		 * 		Leer archivos
		 * 		Aplicar filtros
		 * 		Separar palabras
		 * 		Quitar vacías
		 * 			 
		 * 
		 */
		List<File> Textos = new ArrayList<File>();


		File[] files = new File("/home/sozu00/REC-INF/uni17/Proyecto_Recuperacion/data").listFiles();
		//If this pathname does not denote a directory, then listFiles() returns null. 

		for (File file : files) {
		    if (file.isFile()) {
		        Textos.add(file);
		    }
		}
		
		
		
	}
	
}
