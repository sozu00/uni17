import java.io.File;
import java.nio.file.*;
import java.util.*;

public class Indexing {
	
	public void execute(){
		/*
		 * TODO:
		 * 	Main de Indexacion:
		 * 		Leer archivos
		 * 		Aplicar filtros
		 * 		Separar palabras
		 * 		Quitar vacías
		 * 			 
		 * 
		 */
		File file = new File(AppPath.DATA); 
		String texto;
		
		CadenaFiltros CdF;
		
		CdF.add(new filtroMayusculas());
		CdF.add(new filtroPuntuacion());
		
		if(file.isDirectory()) {
			ArrayList<File> files = file.listFiles();
			for (File f : file) {
		    	texto = new String(Files.readAllBytes(Paths.get(f.getPath())));
		}
		}		

		//List<String> vText = new ArrayList<String>();
		
		
	}
	
}
